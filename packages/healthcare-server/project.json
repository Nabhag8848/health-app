{
  "name": "healthcare-server",
  "tags": ["type:app", "scope:server"],
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/healthcare-server/src",
  "projectType": "application",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --project tsconfig.app.json --noEmit",
        "cwd": "packages/healthcare-server"
      },
      "dependsOn": ["^typecheck"]
    },
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": ["^build"],
      "options": {
        "outputPath": "packages/healthcare-server/dist",
        "main": "packages/healthcare-server/src/main.ts",
        "tsConfig": "packages/healthcare-server/tsconfig.app.json",
        "assets": ["packages/healthcare-server/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false
        }
      }
    },
    "start": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "healthcare-server:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "healthcare-server:build",
          "inspect": false
        },
        "production": {
          "buildTarget": "healthcare-server:build:production",
          "inspect": false
        }
      }
    },
    "migration:deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "typeorm-ts-node-commonjs migration:run -d src/database/datasource/app.datasource.ts",
        "cwd": "packages/healthcare-server"
      }
    },
    "migration:revert": {
      "executor": "nx:run-commands",
      "options": {
        "command": "typeorm-ts-node-commonjs migration:revert -d src/database/datasource/app.datasource.ts",
        "cwd": "packages/healthcare-server"
      }
    },
    "migration:generate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "typeorm-ts-node-commonjs migration:generate src/database/migrations/{args.name} -d src/database/datasource/app.datasource.ts",
        "cwd": "packages/healthcare-server"
      }
    },
    "migration:create": {
      "executor": "nx:run-commands",
      "options": {
        "command": "typeorm-ts-node-commonjs migration:create src/database/migrations/{args.name}",
        "cwd": "packages/healthcare-server"
      }
    },
    "migration:show": {
      "executor": "nx:run-commands",
      "options": {
        "command": "typeorm-ts-node-commonjs migration:show -d src/database/datasource/app.datasource.ts",
        "cwd": "packages/healthcare-server"
      }
    },
    "migration:schema:sync": {
      "executor": "nx:run-commands",
      "options": {
        "command": "typeorm-ts-node-commonjs schema:sync -d src/database/datasource/app.datasource.ts",
        "cwd": "packages/healthcare-server"
      }
    }
  }
}
